import {
    SafeAreaView,
    Text
} from 'react-native';

export default function MoonScreen({ route, navigation }) {
    return (
        <SafeAreaView>
            <Text>Hello</Text>
        </SafeAreaView>
      );
}